import React from 'react';
const ParentageContext = React.createContext({});
export const Provider = ParentageContext.Provider;
export const Consumer = ParentageContext.Consumer;
export default ParentageContext;

